# Stjorna

A simple desktop calendar app for viewing events from multiple scheduling sources in one place.

This is an early prototype. Right now it uses mock data while the main layout and connector model are being built.